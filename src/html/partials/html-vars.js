// Can be used within the HTML like so:
// ${require('../js/partials/html-vars.js').variableName}

module.exports = {
  index: {
    title: 'Homepage Title',
  },
  pageTwo: {
    title: 'Page Two Title',
  }
}
